<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5" style="max-width: 600px;">
    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h3><%= title %></h3>
        </div>
        <div class="card-body">
            <form action="/stages<%= stage ? '/' + stage.id + '?_method=PUT' : '' %>" method="POST">
                
                <div class="mb-3">
                    <label class="form-label">Nom de l'entreprise</label>
                    <input type="text" name="entreprise" class="form-control" value="<%= stage ? stage.entreprise : '' %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Poste visé</label>
                    <input type="text" name="poste" class="form-control" value="<%= stage ? stage.poste : '' %>" required>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">État de la candidature</label>
                        <select name="etat" class="form-select">
                            <option value="En attente" <%= (stage && stage.etat === 'En attente') ? 'selected' : '' %>>En attente</option>
                            <option value="Entretien" <%= (stage && stage.etat === 'Entretien') ? 'selected' : '' %>>Entretien</option>
                            <option value="Validé" <%= (stage && stage.etat === 'Validé') ? 'selected' : '' %>>Validé</option>
                            <option value="Refusé" <%= (stage && stage.etat === 'Refusé') ? 'selected' : '' %>>Refusé</option>
                        </select>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">Date de début</label>
                        <input type="date" name="dateDebut" class="form-control" value="<%= stage ? stage.dateDebut : '' %>">
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">Notes (Contact, adresse...)</label>
                    <textarea name="notes" class="form-control" rows="3"><%= stage ? stage.notes : '' %></textarea>
                </div>

                <div class="d-flex justify-content-between">
                    <a href="/" class="btn btn-secondary">Annuler</a>
                    <button type="submit" class="btn btn-success"><%= stage ? 'Mettre à jour' : 'Enregistrer' %></button>
                </div>
            </form>
        </div>
    </div>
</div>

</body>
</html>