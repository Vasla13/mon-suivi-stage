<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body class="bg-light">

<div class="container mt-5" style="max-width: 600px;">
    <div class="card shadow border-0">
        <div class="card-header bg-dark text-white py-3 d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><%= title %></h5>
            <a href="/" class="btn btn-sm btn-secondary">x</a>
        </div>
        <div class="card-body p-4">
            <form action="/stages<%= stage ? '/' + stage.id + '?_method=PUT' : '' %>" method="POST">
                
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Entreprise</label>
                        <input type="text" name="entreprise" class="form-control" value="<%= stage ? stage.entreprise : '' %>" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Intitulé Stage</label>
                        <input type="text" name="poste" class="form-control" value="<%= stage ? stage.poste : '' %>" required>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label text-muted small"><i class="bi bi-link-45deg"></i> Lien annonce</label>
                    <input type="url" name="lien" class="form-control form-control-sm" placeholder="https://..." value="<%= stage ? stage.lien : '' %>">
                </div>

                <hr class="my-4 text-muted">

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">État</label>
                        <select name="etat" class="form-select">
                            <option value="En attente" <%= (stage && stage.etat === 'En attente') ? 'selected' : '' %>>⏳ En attente</option>
                            <option value="Entretien" <%= (stage && stage.etat === 'Entretien') ? 'selected' : '' %>>🗣️ Entretien</option>
                            <option value="Validé" <%= (stage && stage.etat === 'Validé') ? 'selected' : '' %>>✅ Validé</option>
                            <option value="Refusé" <%= (stage && stage.etat === 'Refusé') ? 'selected' : '' %>>❌ Refusé</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold text-primary">📅 Date de Relance</label>
                        <input type="date" name="dateRelance" class="form-control border-primary" value="<%= stage ? stage.dateRelance : '' %>">
                        <div class="form-text small text-primary">Laisse vide si pas nécessaire.</div>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">Date début stage (Optionnel)</label>
                    <input type="date" name="dateDebut" class="form-control" value="<%= stage ? stage.dateDebut : '' %>">
                </div>

                <div class="mb-4">
                    <label class="form-label">Notes</label>
                    <textarea name="notes" class="form-control" rows="3"><%= stage ? stage.notes : '' %></textarea>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-dark btn-lg"><%= stage ? 'Mettre à jour' : 'Sauvegarder' %></button>
                </div>
            </form>
        </div>
    </div>
</div>

</body>
</html>